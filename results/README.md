# Results artifacts

- `runs/` — raw JSON from experiment and kernel benches (content gitignored)
- `figures/` — generated plots (content gitignored)
- Commit only summaries in `RESULTS.md`, never fabricated numbers
